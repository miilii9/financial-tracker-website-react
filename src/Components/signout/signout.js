import React from 'react'

export default function signout() {
  return (
    <div>sign-out</div>
  )
}
// sign-out
// firebase.auth().signOut().then(() => {
//   // Sign-out successful.
// }).catch((error) => {
//   // An error happened.
// });